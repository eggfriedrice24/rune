---
name: user owns ikako.dev domain
description: User's primary domain is ikako.dev - use it for reverse-DNS identifiers, future site URLs, etc.
type: user
originSessionId: 630c3ac1-31e6-4b00-942d-a2624ea063e0
---

The user owns the domain `ikako.dev`. Use it as the basis for any reverse-DNS application identifier (Tauri app id, macOS bundle id, Android package, etc.) - `dev.ikako.<app>` is the canonical form.

**How to apply**:

- For rune, the Tauri app identifier is `dev.ikako.rune`.
- If a future app under this user needs a bundle id and they don't specify, propose `dev.ikako.<app>` as the default.
- Don't assume `gg.fluxx.*` or other brands unless the user explicitly says so for a given project.
